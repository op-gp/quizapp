{
  "name": "quizapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend --include=dev && npm install --prefix frontend --include=dev && npm run build --prefix backend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "buildAndStart": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
